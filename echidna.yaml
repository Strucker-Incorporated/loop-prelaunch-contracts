# echidna.yaml

# Configuration for specific contract analysis
contract: PrelaunchPointsInvariants

# Specify the Solidity compiler version and settings
solc-args: "--optimize --optimize-runs 200"

# Paths to your source files
sources:
  - src/PrelaunchPoints.sol
  - test/PrelaunchPointsInvariants.sol
