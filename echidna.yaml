# Configuration for Echidna analysis
contract: PrelaunchPointsTest
solc-args: "--optimize --optimize-runs 200"

sources:
  - src/PrelaunchPoints.sol
  - test/PrelaunchPoints.t.sol

contract: PrelaunchPoints0xTest
solc-args: "--optimize --optimize-runs 200"

sources:
  - src/PrelaunchPoints.sol
  - test/PrelaunchPoints0x.test.sol
